package ch.trick17.rolezapps.raytracer.util

import rolez.io.File

object ImageWriter mapped to ImageWriterJava {
    
    mapped def pure write(image: readonly Array[readwrite Array[int]], formatName: String, file: String):
}