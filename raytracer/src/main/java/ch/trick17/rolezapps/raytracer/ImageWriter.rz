package ch.trick17.rolezapps.raytracer

import rolez.io.File

object ImageWriter mapped to ImageWriterJava {
    
    mapped def pure write(image: readonly Array[readwrite Array[int]], formatName: pure String, file: pure String):
}