package ch.trick17.rolezapps.raytracer.util

class VideoWriter mapped to VideoWriterJava {
    
    mapped val width: int
    mapped val height: int
    mapped val framerate: int
    
    mapped new(file: pure String, width: int, height: int, framerate: int, qp: int)
    
    mapped def readwrite writeFrame(image: readonly Array[readwrite Array[int]]):
    mapped def readwrite close:
}